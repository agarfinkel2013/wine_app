  <%= link_to "Add a New Bottle", "/bottles/new" %>

<table>
  <thead>
    <tr>
      <th></th>
      <th>Bottle</th>
      <th>Year</th>
      <th>Color</th>
    </tr>
  </thead>
  <tbody>
  <% @bottles.each do |bottle| %>
    <tr>
      <td>
        <%= link_to "Delete", "bottles/#{bottle.id}", method: 'delete' %>
        <%= link_to "Edit", "bottles/#{bottle.id}/edit" %>
      </td>
      <td><%= link_to bottle.name, "/bottles/#{bottle.id}" %></td>
      <td><%= bottle.year %></td>
      <td><%= bottle.color %></td>
    </tr>
  <% end %>
  </tbody>
</table>
